export const metadata = {
  title: 'Unlock Full Access — DMVSOS',
  description: 'Get unlimited access to all DMV practice tests. All 50 states, 3 categories, 5 languages. Pass your DMV test on the first try.',
};

export default function Layout({ children }) {
  return children;
}
